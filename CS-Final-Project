#include<iostream>
#include <iomanip>
#include <string>

using namespace std;

float calculateAverage(float g_Sci, float g_Math, float g_Eng, float g_Art, float g_hstry){
		average = g_Sci + g_Math + g_Eng + g_Art + g_hstry+ / 5.0;
		}

//this part of the code is needed to find the highest grade out of the 5
float findHighest(float g_Sci, float g_Math, float g_Eng, float g_Art, float g_hstry) {
    float highest = g_hstry;
    if (g_Sci > highest) highest = g_Sci;
    if (g_Math > highest) highest = g_Math;
    if (g_Eng > highest) highest = g_Eng;
    if (g_Art > highest) highest = g_Art;
    return highest;
}

//this part of the code is needed to find the lowest grade out of the 5
	float findLowest(float g_Sci, float g_Math, float g_Eng, float g_Art, float g_hstry)
			if (g_Math>g_Sci && g_Eng> g_Sci && g_Art>g_Sci && g_hstry>g_Sci){
			cout<<"Lowest: Math";
		}
		else if (g_Sci>g_Math && g_Eng>g_Math && g_Art>g_Math && g_hstry>g_Math){
			cout<<"Lowest: SCIENCE";
		}
		else if (g_Sci>g_Eng && g_Math>g_Eng && g_Art>g_Eng && g_hstry>g_Eng){
			cout<<"Lowest: English ";
		}
		else if (g_Sci>g_Art && g_Math> g_Art && g_Eng>g_Art && g_hstry>g_Art){
			cout<<"Lowest: history";
		} 
		else (g_Sci>g_hstry && g_Math>g_hstry && g_Eng>g_hstry && g_Art>g_hstry){
			cout<<"Lowest: SCIENCE";
		}

float getGrade(float average) {
    if (average >= 96) return 1.0;
    if (average >= 94) return 1.25;
    if (average >= 92) return 1.5;
    if (average >= 90) return 1.75;
    if (average >= 88) return 2.0;
    if (average >= 86) return 2.25;
    if (average >= 84) return 2.5;
    if (average >= 82) return 2.75;
    if (average >= 80) return 3.0;
    return 5.0;
}

		int countPassing(float g1, float g2, float g3, float g4, float g5)
	//jadee, kani imohaaa
	

	
	
int main(){
	string name; 
	int id, age, glvl; 
	
		//dont do if else, mag try ta to use a function to check if its a valid input as said in instruction
		// we do gwa converter BEFORE dl checker
	
		//pa change sa variable names plesss like from g1 and g2 to math grade and english grade and yess
		// Thank uuuuu

	cout<<"ENTER NAME: ";
	cin>>name; 
	cout<<"ENTER ID NUMBER: ";
	cin>>id;
	cout<<"ENTER AGE: ";
	cin>>age;
	cout<<"ENTER GRADE LEVEL: ";
	cin>>glvl;
	
	cout<<"ENTER GRADE IN SAID SUBJECTS"<<endl;
	cout<<"MATH: "<<endl;
	cin>>g1; 
	cout<<"SCIENCE: "<<endl;
	cin>>g2; 
	cout<<"ENGLISH: "<<endl;
	cin>>g3; 
	cout<<"HISTORY: "<<endl; 
	cin>>g4;
	cout<<"ART: "<<endl;
	cin>>g5; 
	
	// if subjects input is valid, then proceed to output
	//else, restart using do while loop 
	
	
	
	
	
}
